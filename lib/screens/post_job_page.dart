import 'package:flutter/material.dart';

class PostJobPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Post a Job'),
      ),
      body: Center(
        child: Text('Post your job here'),
      ),
    );
  }
}
